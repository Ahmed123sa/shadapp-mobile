// Superseded by the real test suite under test/unit and test/widget.
// Kept only so `flutter test` never reports zero test files if someone runs
// this single file directly by name.
import 'package:flutter_test/flutter_test.dart';

void main() {
  test('placeholder', () {});
}
